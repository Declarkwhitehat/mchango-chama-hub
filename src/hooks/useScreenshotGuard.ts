/**
 * Screenshot guard has been disabled platform-wide.
 * Screenshots and screen recording are now allowed everywhere in the app.
 * This hook is kept as a no-op to preserve existing imports.
 */
export function useScreenshotGuard() {
  // no-op
}
