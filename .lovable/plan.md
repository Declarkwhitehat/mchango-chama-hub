## Goal
Make `https://pamojanova.com` (Vercel-served, root domain, no `www`) the single primary frontend URL across web + native, and ensure the `.online` domains and `www` variant redirect to it. Supabase URLs untouched.

## Current state
Most code already points to pamojanova, but the **canonical primary is currently `https://www.pamojanova.com`** (with `www`). You asked for the root `https://pamojanova.com`. There are no remaining `lovable.app` / `lovableproject.com` references in app code.

## Changes

### 1. Flip canonical primary from `www.pamojanova.com` → `pamojanova.com`
- `src/lib/publicUrl.ts` — `PUBLIC_BASE_URL = 'https://pamojanova.com'`. Keep `www.pamojanova.com`, `pamojanova.online`, `www.pamojanova.online` in `KNOWN_PUBLIC_HOSTS` so any incoming variant is recognized but rewritten to the root domain.
- `src/components/SEO.tsx` — `PRIMARY_ORIGIN = 'https://pamojanova.com'`.
- `index.html` — update `<link rel="canonical">`, `og:url`, `twitter` URL, and JSON-LD `url`/`logo` to `https://pamojanova.com/...`.
- `public/sitemap.xml` and `android/app/src/main/assets/public/sitemap.xml` — rewrite all `<loc>` entries to `https://pamojanova.com/...`.
- `android/app/src/main/assets/public/index.html` — same SEO/meta updates as `index.html`.

### 2. Redirect `www` and `.online` → root domain
Update the inline redirect script in `index.html`:
```js
if (h === 'www.pamojanova.com' || h === 'pamojanova.online' || h === 'www.pamojanova.online') {
  window.location.replace('https://pamojanova.com' + path + search + hash);
}
```
Mirror in `android/app/src/main/assets/public/index.html`.

### 3. Capacitor / native shell
- `capacitor.config.ts` — already `https://pamojanova.com` (no change needed, confirm).
- `android/app/src/main/assets/capacitor.config.json` — already `https://pamojanova.com` (regenerated by `npx cap sync`; leave as-is).
- `android/app/src/main/AndroidManifest.xml` — keep all four hosts (`pamojanova.com`, `www.pamojanova.com`, `pamojanova.online`, `www.pamojanova.online`) as deep-link intent filters so links shared from anywhere still open the app; the in-app handler then rewrites them.

### 4. Local share URL helpers using `window.location.origin`
- `src/pages/MchangoList.tsx` and `src/pages/OrganizationList.tsx` — replace `${window.location.origin}/...` with `publicUrls.exploreMchango()` / `publicUrls.organizationsList()` so shared links always use `https://pamojanova.com` even from the native shell or `.online`.

### 5. Memory
Update `mem://identity/platform-branding-and-leadership` (or add a small `mem://infrastructure/primary-domain` note): primary domain = `https://pamojanova.com` (root, no `www`); served by Vercel; `.online` and `www` redirect to it.

## Out of scope (not changed)
- Supabase URL, anon key, Edge Function URLs, callback URLs.
- Auth/OAuth `redirectTo` (none in code currently).
- DNS — you set those at the registrar; Lovable cannot edit them.

## Notes
- After merging, you must run `npx cap sync android` locally and rebuild the APK so the native shell picks up `capacitor.config.json` (already correct, but sync regenerates assets/sitemap/index.html copy).
- Make sure your Vercel project has both `pamojanova.com` (primary) and `www.pamojanova.com` (redirect → root) configured, otherwise the JS redirect is the only fallback.
